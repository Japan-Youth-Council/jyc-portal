# 政策ナレッジ 企画書

参照：[00_企画.md](./00_企画.md) ／ DevWiki 開発指示書（Wiki UX の原案）

---

## 1. 位置づけ

**政策ナレッジ**（`/knowledge`）は、JYC Portal の 4 機能のうち「政策知見の蓄積」を担う画面である。政策テーマごとのドキュメント・マニュアル・ノウハウを共同編集し、属人化しがちな知見を残す。

本企画は、スタンドアロンの Wiki アプリ（DevWiki）を新規構築するものではない。**既存の `/knowledge` を、DevWiki が示す閲覧・編集体験に寄せて再設計する**。認証・データ永続化・カテゴリ階層は JYC Portal の現行仕様を正とする。

| 層 | 方針 |
| --- | --- |
| 体験（閲覧／編集、Markdown、目次、検索） | DevWiki を採用し、現状のプレーンテキスト編集から置き換える |
| データと権限 | 既存の Supabase（`policy_categories` / `knowledges`）とログイン必須を維持する |
| 見た目 | ポータル全体のヘッダー・日本語 UI・ライト基調に合わせる。独立したダークモードは当面入れない |

ホーム画面ではいま「準備中」と案内しているが、画面自体は既に動いている。本企画の完了をもって、ホーム表記も利用可能な機能として揃える。

---

## 2. 解決したい課題

協議会の政策知見は、委員会やプロジェクト、個人のメモに散らばりやすい。現行の政策ナレッジは「大項目 → 小項目にドキュメントを置く」枠組みはあるが、次の理由で知見ベースとして使いづらい。

- **書くことと読むことが分かれていない**。本文は常にテキストエリアで、見出し・箇条書き・表が整形されない
- **長い文書を辿れない**。目次がなく、パンくずも弱い
- **横断検索が弱い**。検索は選択中の小項目内のタイトル・本文のみ。タグもない
- **削除がない**。誤作成したドキュメントを片付けられない
- **作業者名を毎回手入力する**。ログイン済みプロフィールと結びついていない

DevWiki の「サイドバーで探す → 閲覧で読む → 分割エディタで書く」という流れを、既存の 2 階層カテゴリと Supabase 保存の上に載せることで、これらを解消する。

---

## 3. 現状と DevWiki 仕様の対照

### 3.1 現行実装（JYC Portal）

`app/knowledge/page.tsx` が 1 ファイルで完結している。

- 大項目タブ → 小項目タブ → ドキュメントカードの縦並び
- タイトル・本文はインラインの `input` / `textarea`。blur で Supabase 更新
- 検索は選択中の小項目内のみ（タイトル・本文の部分一致）
- 作成時のタイトルは「無題のドキュメント」。削除 UI なし
- 編集の前提として画面上部に作業者名の手入力が必須
- Markdown ライブラリは未導入。閲覧モード／編集モードの切替なし

データ型（`types/database.ts`）：

```
PolicyCategory  id, name, parent_id, created_by_name, created_at
Knowledge       id, category_id, title, content, author_name, updated_at, created_at
```

カテゴリは `parent_id === null` が大項目、それ以外が小項目。ドキュメントは小項目にぶら下がる。

### 3.2 DevWiki が求める体験

- サイドバー：カテゴリ折りたたみ、タグクラウド、タイトル／本文／タグのインクリメンタル検索。モバイルは引き出し
- 閲覧：Markdown レンダリング（H1〜H3、強調、リスト、引用、コード、表）、右カラム TOC、パンくずと更新日時・タグ
- 編集：左右分割（Raw Markdown ＋ リアルタイムプレビュー）、挿入ツールバー、タイトル／カテゴリ／タグのメタデータ編集
- 保存：LocalStorage の CRUD。将来は Supabase 等
- テーマ：Tailwind `dark` によるダークモード

データ型の想定は `WikiPage`（id, title, category, tags, content, updatedAt）。カテゴリは文字列 1 本、タグは配列。

### 3.3 採用・改変・見送り

| DevWiki 項目 | 判断 | 政策ナレッジでの扱い |
| --- | --- | --- |
| サイドバー（カテゴリ／検索／モバイル引き出し） | 採用（改変） | 大項目→小項目の 2 階層ツリーをサイドバーに移す。ポータル Header のハンバーガーとは別物として、ナレッジ内サイドバーを持つ |
| タグクラウドとタグフィルタ | 採用 | `knowledges` にタグを追加。カンマ区切り入力、クリックで絞り込み |
| 横断インクリメンタル検索 | 採用 | カテゴリ選択に依存せず、タイトル・本文・タグを対象にする |
| 閲覧モード（Markdown） | 採用 | `content` を Markdown として保存・描画する |
| 自動 TOC（H1〜H3） | 採用 | 閲覧時の右カラム。狭い画面では本文上部または省略 |
| パンくず・メタ情報 | 採用 | 大項目 / 小項目 / タイトル、更新日時、最終更新者、タグ |
| 分割エディタ＋挿入ツールバー | 採用 | 閲覧と編集を明示切替。狭い画面は上下分割またはプレビュー切替 |
| メタデータ編集（タイトル・カテゴリ・タグ） | 採用 | カテゴリは既存の小項目選択（大→小）。自由入力の単一カテゴリ文字列にはしない |
| LocalStorage と `useWikiPages` | 見送り | 永続化は現行どおり Supabase。フック名は `useKnowledges` 等、既存テーブルに合わせる |
| ダークモード | 見送り | ポータル全体がライト基調。単独導入するとヘッダーと不整合になる |
| スタンドアロン Header / 独自認証 | 見送り | 既存の `Header` と `AuthGuard` を使う |
| ドキュメント削除 | 追加（DevWiki CRUD 相当） | 現行に無いため、閲覧／編集から削除できるようにする |
| 作業者名の手入力 | 改変 | ログイン中プロフィールの氏名を最終更新者にする。未取得時のみ入力欄を残す |
| カテゴリの追加 | 維持 | サイドバーから大項目・小項目を追加できる現状を残す |

---

## 4. 目指す体験

メンバーは政策テーマを「探す → 読む → 直す」の順で使う。

1. **探す**  
   左サイドバーで大項目を開き、小項目を選ぶ。または検索欄にキーワードを入れてタイトル・本文・タグから絞り込む。タグクラウドからテーマ横断で辿ることもできる。
2. **読む**  
   選択したドキュメントは整形された Markdown として表示される。右側（または本文脇）の目次から見出しへ飛ぶ。パンくずに大項目・小項目が見える。
3. **直す**  
   「編集」で左右（または上下）分割のライブエディタに入る。ツールバーで見出し・リスト・リンク等を挿入し、プレビューを見ながら書く。保存で即時に Supabase へ反映し、閲覧へ戻る。
4. **足す／捨てる**  
   小項目を選んだ状態で新規ドキュメントを作る。不要なものは削除する。カテゴリの追加は現状どおりサイドバーから行う。

対象ユーザーは [00_企画.md](./00_企画.md) と同じ。一般メンバーもコアメンバーも、ナレッジの閲覧・作成・編集・削除ができる（現行と同じ権限）。カテゴリ追加も同様に全メンバー可とする。権限の段階化は本企画の対象外。

---

## 5. 機能要件

### 5.1 ナビゲーション（サイドバー）

- PC：左固定。大項目ごとに折りたたみ、配下の小項目を列挙。小項目を選ぶと、その配下のドキュメント一覧がサイドバー内またはメイン先頭に出る
- モバイル：オーバーレイの引き出し。ポータル全体メニュー（Header）とは独立して開閉する
- タグクラウド：登録済みタグを列挙。クリックでそのタグを持つドキュメントに絞る。再クリックで解除
- 検索：入力のたびにタイトル・本文・タグを部分一致。カテゴリ未選択でも全件を対象にする
- 大項目・小項目の追加 UI はサイドバーに残す

### 5.2 閲覧モード

- Markdown を描画する。少なくとも見出し（H1〜H3）、太字・斜体、箇条書き・番号リスト、引用、インライン／ブロックコード、テーブル、リンク
- 本文中の H1〜H3 から目次を自動生成し、アンカーリンクでスクロールする
- パンくず：大項目 › 小項目 › ドキュメントタイトル
- メタ：更新日時、最終更新者（プロフィール氏名）、タグ
- 操作：「編集」「削除」（削除は確認ダイアログ）

### 5.3 編集モード

- 「編集」で入り、「保存」または「キャンセル」で閲覧に戻る。保存失敗時は編集を維持し、理由を示す
- 広い画面：左が Raw Markdown、右がリアルタイムプレビュー
- 狭い画面：エディタとプレビューのタブ切替、または上下分割
- ツールバー：太字、斜体、見出し、リスト、リンク、コードを、キャレット位置へ挿入する
- メタデータ：タイトル、所属小項目（大項目を選んでから小項目）、タグ（カンマ区切り）。カテゴリ変更でドキュメントの所属を移せる

### 5.4 データ永続化

- 作成・取得・更新・削除は Supabase に即時反映する。LocalStorage は使わない
- 新規作成時の初期タイトルは「無題のドキュメント」、本文は空。作成後は編集モードで開く
- 最終更新者はログイン中プロフィールの氏名を書き込む

### 5.5 ポータルとの接続

- ルートは現行どおり `/knowledge`。ログイン必須
- ホームの政策ナレッジカードから「（準備中）」を外す
- 既存の `content` はプレーンテキストのまま残っている。Markdown として解釈しても破綻しにくい（改行は段落または `remark-break` 相当で維持する）

---

## 6. 画面構成

ポータル Header の下に、ナレッジ専用の 2 ペインを置く。

```
Header（既存：Home / メンバー / プロジェクト / コンタクト / 政策ナレッジ / プロフィール）

/knowledge
  ├── サイドバー（モバイル時は引き出し）
  │     ├── 検索
  │     ├── 大項目（折りたたみ）
  │     │     └── 小項目 → ドキュメント
  │     └── タグクラウド
  └── メイン
        ├── 未選択：検索結果または「カテゴリ／検索から選んでください」
        ├── 閲覧：パンくず・メタ・本文（wiki-prose）・TOC
        └── 編集：ツールバー・メタフォーム・分割エディタ
```

DevWiki モックの骨格（`sidebar` / `view-mode` / `edit-mode` / `toc-container`）は、コンポーネント分割の参照とする。ページファイルを巨大な 1 枚にしない。

想定コンポーネント：

| コンポーネント | 役割 |
| --- | --- |
| `KnowledgeLayout` | サイドバー＋メインの枠、モバイル開閉 |
| `KnowledgeSidebar` | 検索、カテゴリツリー、タグ、カテゴリ追加 |
| `ArticleView` | パンくず、メタ、Markdown 本文 |
| `TableOfContents` | 見出しから目次 |
| `MarkdownEditor` | ツールバー、textarea、プレビュー |
| `KnowledgeMetaForm` | タイトル、カテゴリ、タグ |

---

## 7. データモデル

既存テーブルを拡張する。DevWiki の `WikiPage.category`（文字列）は使わず、現行のカテゴリツリーを正とする。

### 7.1 変更しないもの

- `policy_categories` … 大項目／小項目の 2 階層
- `knowledges.category_id` … 小項目への紐づけ
- `knowledges.title` / `content` / `updated_at` / `created_at`

### 7.2 追加するもの

| 対象 | 内容 |
| --- | --- |
| `knowledges.tags` | `text[]`。空配列可。UI ではカンマ区切り |
| `knowledges.author_name` | 残す。手入力ではなくプロフィール氏名を格納する運用に変える |

タグ用の中間テーブルは、初期は作らない。件数・同時編集の規模がポータル内部向けであるため、配列で足りる。タグ名の正規化（前後空白の削除、空要素の除去）はアプリ側で行う。

### 7.3 フロントの型（現行の拡張）

```
Knowledge
  id, category_id, title, content,
  tags: string[],
  author_name, updated_at, created_at
```

DevWiki の `WikiPage` は導入しない。命名は既存の `Knowledge` / `PolicyCategory` に合わせ、フックは Supabase 同期の `useKnowledges`（仮）とする。

---

## 8. 技術構成

ポータル全体のスタックに乗せる。DevWiki 推奨のうち、既に揃っているものは流用する。

| 項目 | 本企画での選択 |
| --- | --- |
| フレームワーク | Next.js（App Router）+ TypeScript。現行どおりクライアントページ中心 |
| スタイル | Tailwind CSS。本文は `prose` 相当のクラス（独自 `wiki-prose` でも可） |
| アイコン | lucide-react（導入済み。FontAwesome は足さない） |
| Markdown | `react-markdown` + `remark-gfm`（テーブル・打ち消し線）。必要なら改行維持用プラグイン |
| 状態 | React Hooks。ページ一覧・選択 ID・検索・タグフィルタ・閲覧／編集モード |
| 保存 | Supabase クライアント（`knowledges` / `policy_categories`）。LocalStorage 同期は実装しない |
| 認証 | 既存 `AuthGuard`。最終更新者は `profiles` の氏名 |

コンポーネントは `components/knowledge/` 配下に置き、`app/knowledge/page.tsx` は組み立てに徹する。

---

## 9. 既存仕様との整合

- **閉じたシステム**：公開ページは増やさない。`/knowledge` はログイン後のみ
- **コアメンバー判定**：ナレッジの編集権限には使わない（現行どおり全員編集可）
- **日本語 UI**：ラベル・空状態・確認ダイアログは日本語
- **レスポンシブ**：Header のモバイルメニューと、ナレッジサイドバーの引き出しが同時に開かないよう、ナレッジ側はページ内状態で管理する
- **ホーム不整合の解消**：政策ナレッジカードの「（準備中）」を削除する。コンタクト入口が `/advocacy` の件は本企画の対象外

---

## 10. 実装ステップ

DevWiki 指示書の Step 1〜4 を、Supabase 前提に読み替えた順序。

**Step 1 — レイアウトとコンポーネント分割**  
`KnowledgeLayout` / `KnowledgeSidebar` / `ArticleView` / `MarkdownEditor` / `TableOfContents` の枠を切る。見た目は現行ポータル（灰背景・白カード・青アクセント）に合わせる。

**Step 2 — 型とデータフック**  
`Knowledge` に `tags` を足し、マイグレーションする。`useKnowledges` で一覧取得と CRUD（Supabase）。プロフィール氏名を最終更新者に使う。

**Step 3 — Markdown 閲覧と分割エディタ**  
閲覧のレンダリング、編集のスプリットビュー、ツールバーのキャレット挿入、メタデータフォーム。保存／キャンセル／削除。

**Step 4 — TOC・検索・タグ**  
見出しからの目次、横断検索、タグクラウドとフィルタ。ホームの「準備中」削除。

各ステップの完了条件は、ログイン済みメンバーが PC とスマホの両方で「探す → 読む → 直す」が通ること。

---

## 11. 非対象・今後の余地

本企画ではやらない。

- LocalStorage 版 Wiki、ダークモード、独立した Wiki 用ヘッダー
- 同時編集（OT / CRDT）、版管理・差分表示、下書きと公開の分離
- 画像アップロード（プロフィール写真用 Drive とは別系統になるため、本文は URL リンクまで）
- カテゴリの並び替え・統合・削除、ドキュメントの閲覧権限の段階化
- コアメンバーのみカテゴリ追加、などの権限分割

これらは、政策ナレッジが「Markdown で読めて書ける共有 Wiki」として定着したあとの段階とする。
