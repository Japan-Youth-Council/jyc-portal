# JYC Portal 企画概要

## 1. プロダクト概要

**JYC Portal**（Japan Youth Council Portal System）は、日本若者協議会（Japan Youth Council）のメンバー専用ポータルである。

政策委員会・地方支部・各種プロジェクトの進行状況を一元管理し、メンバー同士の顔合わせ、提言・交渉の記録共有、政策ナレッジの蓄積を支援する。

利用規約上、対象は原則として当団体の会員、または当団体が利用を許可した関係者に限る。内部の個人情報や未公開プロジェクト情報を扱うため、ログイン後のみ利用できる閉じたシステムとして設計している。

---

## 2. 解決したい課題

協議会は、政策委員会・地方支部・横断プロジェクトなど複数の活動単位が並行して動く。メンバーは全国に分散しており、次のような情報が散在しやすい。

- 誰がどの委員会・支部・プロジェクトに参加しているか
- 各プロジェクトの目的、進捗、連絡先リンク
- 政党・省庁・団体との面談やヒアリングの経緯
- 政策テーマごとの知見・マニュアル

JYC Portal は、これらを一つの Web アプリに集約し、メンバー間の円滑なコミュニケーションと活動継続を支える。

---

## 3. 対象ユーザー

| 役割 | 想定 | できること |
| --- | --- | --- |
| 一般メンバー | 協議会会員 | 自己紹介の閲覧・自身のプロフィール編集、プロジェクト閲覧、コンタクト履歴の記録・閲覧、ナレッジの閲覧・編集 |
| コアメンバー | `@japanyouthcouncil.com` ドメインのメールで登録したユーザー | 一般メンバーの機能に加え、政策委員会・地方支部・大プロジェクト・小プロジェクトの追加・名称変更、概要編集、ステータス変更 |
| 未ログイン利用者 | 一般公開 | トップページ、利用規約、プライバシーポリシーのみ閲覧可能 |

コアメンバー判定は、プロフィール登録・更新時にメールアドレスが `@japanyouthcouncil.com` で終わるかどうかで自動設定される。

---

## 4. 提供価値

1. **顔が見える組織運営**  
   写真・所属・活動目標付きの自己紹介により、面識のないメンバー同士でもつながりやすくする。
2. **活動単位の可視化**  
   政策委員会・地方支部・大プロジェクト・小プロジェクトを階層で管理し、進行中／停止中／終了済みを区別する。
3. **対外活動の記憶の継承**  
   政党・省庁・地方・個人とのコンタクトを時系列で残し、資料・議事録・HP記事と紐づける。
4. **政策知見の蓄積**  
   テーマ別カテゴリのもとでドキュメントを共同編集し、属人化しがちなノウハウを残す。

---

## 5. 主要機能

### 5.1 認証・アカウント

- Google アカウントによる OAuth ログイン
- 未ログイン時はトップ（`/`）へ誘導。ログイン済みかつプロフィール未登録なら初回設定（`/setup-profile`）へ強制遷移
- プロフィール登録済みならトップ／初回設定画面には入れず、ホーム（`/home`）へ誘導
- プロフィール設定画面では、パスワード再確認後にメールアドレス・パスワード変更が可能
- パスワード再設定画面（`/reset-password`）あり

### 5.2 メンバー自己紹介（`/members`）

- 全メンバーのプロフィールをギャラリー形式で表示
- 名前・ふりがな検索、コアメンバー絞り込み、政策委員会／地方支部／大プロジェクトでの絞り込み
- 並び順は「ランダム」または「最近登録した人」
- カードをクリックすると詳細モーダル（目標、所属、外部活動、SNS、自由記述）を表示
- 終了済みプロジェクトの所属タグは、トグルで表示／非表示を切り替え可能

### 5.3 プロジェクト一覧（`/projects`）

活動単位は次の階層で管理する。

```
政策委員会 / 地方支部 / 大プロジェクト
        └── 小プロジェクト
```

一覧画面は 3 列構成。

- 左：事務局機能、地方支部
- 中央：政策委員会
- 右：直轄・その他プロジェクト、大プロジェクト

できること：

- 名前検索（委員会・支部・大／小プロジェクトを横断）
- 終了済みの表示切替
- コアメンバーによる親プロジェクトの追加・名称編集
- 詳細画面での概要・リンク編集、ステータス変更（進行中／停止中／終了済み）
- 親プロジェクト配下への小プロジェクト追加

各プロジェクトは「非公開（ロック）」フラグを持てる。小プロジェクト詳細には、将来のタスク管理を置く想定がある（未実装）。

### 5.4 コンタクト履歴（`/contacts`）

政党や省庁、団体、個人との提言・交渉ログを残す CRM 的な機能。

- コンタクト先はカテゴリ分け：国政、中央行政、地方、個人、その他
- 地方はさらに地域でグループ化
- コンタクト先の追加、改名、削除、統合（履歴を別先へ移して元を削除）、ドラッグによる並び替え
- 履歴カード：実施日、件名、先方出席者、JYC 出席者、メモ、資料 URL、議事録 URL、HP 記事 URL
- 先方出席者を登録すると「個人」カテゴリのコンタクト先と自動連携し、同じ記録が双方から見える
- 件名・内容・出席者での横断検索

### 5.5 政策ナレッジ（`/knowledge`）

- 大項目 → 小項目のカテゴリツリー（サイドバー。モバイルは引き出し）
- ドキュメントは Markdown で閲覧し、分割エディタで編集する
- タイトル・本文・タグの横断検索と、タグクラウドによる絞り込み
- 目次、パンくず（大項目 › 小項目 › タイトル）、更新日時・最終更新者
- 最終更新者はログイン中プロフィールの氏名を使う。作成・削除も可能

### 5.6 プロフィール

初回設定・後からの編集で、次の情報を登録する。

| 必須 | 任意 |
| --- | --- |
| 名前、ふりがな、属性（高校生／大学生／大学院生／社会人／その他）、都道府県、実現したいこと | 写真、市区町村、所属委員会・支部・プロジェクト、外部活動、SNS、自由記述 |

プロフィール写真はブラウザ側で圧縮したうえで Google Drive にアップロードし、公開 URL を保存する。

---

## 6. 画面構成と導線

```
未ログイン
  └── /            ランディング＋ Google ログイン
        ├── /terms     利用規約
        └── /privacy   プライバシーポリシー

ログイン直後（氏名未登録）
  └── /setup-profile   初回プロフィール設定

ログイン済み
  ├── /home            ダッシュボード（4機能への入口）
  ├── /members         メンバー自己紹介
  ├── /projects        プロジェクト一覧
  │     ├── /projects/{committee|branch|major}/[id]  親プロジェクト詳細
  │     └── /projects/detail/[id]                    小プロジェクト詳細
  ├── /contacts        コンタクト履歴
  ├── /knowledge       政策ナレッジ
  ├── /profile         プロフィール・アカウント設定
  └── /reset-password  パスワード再設定
```

ヘッダー（PC／スマホ対応）から Home、メンバー自己紹介、プロジェクト一覧、コンタクト履歴、政策ナレッジ、プロフィール、ログアウトにアクセスできる。

---

## 7. 技術構成

| 層 | 内容 |
| --- | --- |
| フロントエンド | Next.js（App Router）、React、TypeScript、Tailwind CSS |
| 認証・DB | Supabase（Auth + PostgreSQL）。クライアントから直接テーブルを読み書き |
| 画像 | Google Drive API（プロフィール写真のアップロード／取得） |
| UI | lucide-react アイコン、レスポンシブ（PC ナビ＋スマホハンバーガー） |

主なデータテーブル（実装から読み取れるもの）：

- `profiles` … メンバープロフィール
- `policy_committees` / `local_branches` / `major_projects` / `projects` … プロジェクト階層
- `contact_targets` / `contact_records` … コンタクト先と活動記録
- `policy_categories` / `knowledges` … ナレッジの分類と本文

公開ページは `/`、`/privacy`、`/terms` のみ。それ以外はセッション必須。

---

## 8. 非機能・運用上の前提

- **守秘**：利用規約で、他メンバーの個人情報や未公開プロジェクト情報の外部漏洩を禁止している
- **個人情報**：Google アカウント情報（メール、氏名、画像）と、ユーザーが入力した居住地・所属・目標などを取得。ログイン認証、プロジェクト運営、メンバー間コミュニケーションに利用する
- **デバイス**：PC とスマートフォンの両方を想定したレイアウト
- **言語**：日本語 UI

---

## 9. 現状と今後の余地

実装済みの中核は、メンバー可視化、プロジェクト階層管理、コンタクト履歴、ナレッジ編集である。コード上、次のような拡張余地・不整合が残っている。

- 小プロジェクト詳細のタスク管理は未実装（コメントで将来追加と明記）
- ホームのコンタクト入口が `/advocacy` を指している一方、実ページとヘッダーは `/contacts`
- プロジェクトの「非公開」表示はあるが、ロック時の閲覧制限ロジックは画面上では限定的

本企画の当面の完成イメージは、**協議会内部の「人・活動・対外折衝・知見」を一つのポータルで回せる状態**である。タスク管理や閲覧権限の精緻化は、その次の段階として位置づける。
